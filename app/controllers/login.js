import Controller from '@ember/controller';
import { observer } from '@ember/object';

export default Controller.extend({
});

// Impredecibles
// No son explícitos
// No son determinísticos (hacen que tu aplicación no sea determinística)